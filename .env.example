# =============================================================================
# Environment Configuration for tor-socks-proxy-service
# =============================================================================
# Copy this file to .env and configure for your environment
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   docker-compose up -d
#
# For Linux with cadvisor:
#   docker-compose --profile linux up -d
# =============================================================================

# --- Project Isolation ---
# Unique name to isolate containers, networks, volumes
# Use different names for multiple environments on same server
COMPOSE_PROJECT_NAME=tor-proxy-dev

# --- Image Tags ---
# Tag for locally built images (dev, stage, prod)
ENV_TAG=dev

# --- Network Binding ---
# IP address to bind services
# - Use specific IP for multi-environment on same server
# - Use 0.0.0.0 for single environment (binds to all interfaces)
# - Use 127.0.0.1 for local-only access (macOS typical)
BIND_IP=0.0.0.0

# --- Service Ports ---
# External ports for each service
TOR_PORT=9050
API_PORT=8000
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
CADVISOR_PORT=8080

# --- Credentials ---
# Grafana admin password
GRAFANA_PASSWORD=admin

# =============================================================================
# Example Configurations
# =============================================================================
#
# --- Linux Production (Server IP: 10.0.0.1) ---
# COMPOSE_PROJECT_NAME=tor-proxy-prod
# ENV_TAG=prod
# BIND_IP=10.0.0.1
# TOR_PORT=9050
# API_PORT=8000
# PROMETHEUS_PORT=9090
# GRAFANA_PORT=3000
# CADVISOR_PORT=8080
# GRAFANA_PASSWORD=secure_password_here
#
# --- Linux Development (Server IP: 10.0.0.2, same server as prod) ---
# COMPOSE_PROJECT_NAME=tor-proxy-dev
# ENV_TAG=dev
# BIND_IP=10.0.0.2
# TOR_PORT=9050
# API_PORT=8000
# PROMETHEUS_PORT=9090
# GRAFANA_PORT=3000
# CADVISOR_PORT=8080
# GRAFANA_PASSWORD=admin
#
# --- macOS Stage (localhost) ---
# COMPOSE_PROJECT_NAME=tor-proxy-stage
# ENV_TAG=stage
# BIND_IP=127.0.0.1
# TOR_PORT=9050
# API_PORT=8000
# PROMETHEUS_PORT=9090
# GRAFANA_PORT=3000
# CADVISOR_PORT=8080
# GRAFANA_PASSWORD=admin
#
# =============================================================================
# Notes
# =============================================================================
# - cadvisor only works on Linux (use --profile linux)
# - Logs: use `docker-compose logs -f [service]` to view
# - On deploy, .env is preserved (not overwritten from repo)
# =============================================================================
